name: SundialKitBinary
settings:
  LINT_MODE: ${LINT_MODE}
packages:
  SundialKitBinary:
    path: .
aggregateTargets:
  Lint:
    buildScripts:
      - path: Scripts/lint.sh
        name: Lint
        basedOnDependencyAnalysis: false
    schemes: {}
