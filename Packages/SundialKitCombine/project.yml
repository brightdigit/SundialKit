name: SundialKitCombine
settings:
  LINT_MODE: ${LINT_MODE}
packages:
  SundialKitCombine:
    path: .
aggregateTargets:
  Lint:
    buildScripts:
      - path: Scripts/lint.sh
        name: Lint
        basedOnDependencyAnalysis: false
    schemes: {}
