name: SundialKitStream
settings:
  LINT_MODE: ${LINT_MODE}
packages:
  SundialKitStream:
    path: .
aggregateTargets:
  Lint:
    buildScripts:
      - path: Scripts/lint.sh
        name: Lint
        basedOnDependencyAnalysis: false
    schemes: {}
